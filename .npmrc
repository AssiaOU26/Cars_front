# Skip optional dependencies to avoid platform-specific package issues
optional=false
# Force npm to use only production dependencies
production=false
# Disable platform-specific binary downloads
fund=false
audit=false
